services:
  db:
    image: pgvector/pgvector:pg18
    env_file:
      - .env  
    ports:
      - "5432:5432"

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    env_file:
      - .env 
    depends_on:
      - db
    ports:
      - "8000:8000"
