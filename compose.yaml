services:
  db:
    image: postgres:18-alpine
    env_file:
      - .env  
    ports:
      - "5432:5432"

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    env_file:
      - .env 
    depends_on:
      - db
    ports:
      - "8000:8000"
